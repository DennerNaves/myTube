import styled from 'styled-components';
import jsdata from '../../../../jsdata.json';

const StyledBanner = styled.div`
    background-image: url(${jsdata.background});
    height: 230px;
`

export default StyledBanner;
